fix(chat): kill three snapshot misreads (empty-day, this-week, exclusion-as-problem) (SW v418)

Three hallucination modes the AI produced on a fresh thread, all traceable to snapshot wording:

1. Day 3 "is actually an empty accessory day" despite assistance being scheduled there. Day header
   said "accessory day" and the model pattern-matched "empty". FIX: three explicit cases now —
   "main lifts: X" / "accessory-only day (N assistance movements scheduled — listed below)" /
   "EMPTY (no main lifts and no assistance)".

2. "Week 2 (this week)" on a Sunday — AI conflated calendar week with block training week.
   FIX: snapshot appends TODAY date + weekday + next-scheduled block week (from schedule cursor)
   and tells AI to ASK before assuming "this week" / "next week".

3. AI claimed the block contains Skull Crusher and Close-Grip Bench Press, both of which are
   actually on the user's exclusion list. It hallucinated them INTO the block FROM the
   exclusion list. WORST of the three. FIX: exclusions section renamed to "Movement
   exclusions (USER FILTERS — NOT in the block plan)" with explicit "never claim these are
   in the block" guidance + system-prompt rule that reads "verify a movement's presence by
   reading the plan section, never by inverting the filter list."

Files:
- apps/web/src/lib/useChat.ts: day header rewrite + today/next-up disambiguation + schedule cursor pulled.
- packages/domain/src/chat-context.ts: exclusions section renamed + reframed.
- apps/api/src/functions/chat.ts: HARD EXCLUSIONS bullet rewritten in SYSTEM_PROMPT_BASE.

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>
