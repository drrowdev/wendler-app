{
  "name": "@wendler/db-schema",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "echo \"(no tests yet)\"",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"(no lint yet)\""
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
